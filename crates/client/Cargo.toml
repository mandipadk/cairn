[package]
name = "ambolt-client"
version.workspace = true
edition = "2024"
rust-version.workspace = true
# The pieces other tools embed - the MCP adapter and the claim runner -
# are permissively licensed on purpose. The forge itself is AGPL-3.0.
license = "Apache-2.0"
repository.workspace = true
description = "Client side of the Ambolt forge protocol: the MCP adapter for agents and the claim runner"

[dependencies]
anyhow = "1.0.104"
serde_json = "1.0.151"
ureq = { version = "3.4.0", features = ["json"] }
ring = "0.17"
base64 = "0.23.1"

[lints]
workspace = true
