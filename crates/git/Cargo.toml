[package]
name = "cairn-git"
version.workspace = true
edition = "2024"
license.workspace = true
repository.workspace = true

[dependencies]
bytes = "1"
futures-core = "0.3"
thiserror = "2.0.20"
tokio = { version = "1.53.1", features = ["process", "io-util", "fs", "rt", "time"] }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
